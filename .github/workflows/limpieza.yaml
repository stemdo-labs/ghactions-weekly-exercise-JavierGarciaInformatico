name: limpieza

on:
  workflow_call:  

jobs:
  limpieza-docker:
    runs-on: [weeklyjaviergarcia]
    if: always() 
    steps:
      - name: Eliminar todos los contenedores
        run: docker rm -f $(docker ps -a -q)

      - name: eliminar todas las imágenes
        run: docker system prune -a